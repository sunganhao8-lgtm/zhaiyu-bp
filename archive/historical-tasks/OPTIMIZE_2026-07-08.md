<!-- 部分执行（2026-07-08 ~ 2026-07-10 部分视觉/CSS 已改）→ 2026-07-12 由 DEC-029 收口 -->

# bp.html 全面优化任务

你是专业的 BP 文档 + CSS 工程师。直接修改 `bp.html`，目标是：**投资人打开 3 秒内能抓住核心信息，阅读全程无视觉疲劳**。

## 硬约束
1. 只改 `bp.html`，不动其他文件
2. 保持现有品牌色（暖橙+深蓝）、2D 扁平风格不变
3. 保持所有 tab 结构、全屏/幻灯片功能 intact
4. 不要引入外部依赖
5. 中文正文，技术术语保留英文

---

## 第一部分：CSS 清理（减少 40%+ CSS 体积）

### 1. 删除死 CSS（display:none 的代码块）
`.brand-bar::before` 第 101-130 行 → display:none 后跟着 30 行不会生效的 background-image。删掉整个 `brand-bar::before` 和 `brand-bar::after` 的 display:none 块。

### 2. 合并重复定义
- `.brand-center` 出现 **2 次**（行 166 + 行 245）。合并为一个，后一个替代前一个
- `.brand-center-title` 出现 **3 次**（行 188, 222, 1039）。合并为一个
- `.brand-center:hover .brand-center-logo` 出现 **2 次**（行 260 + 1231）。保留一个

### 3. 合并重复媒体查询
有两个 `@media(max-width:1024px)` 块。合并为一。

### 4. 删除无用的代码
- `/* 品牌栏动效容器 - 已废弃 */` 相关 CSS
- `.z-logo{display:none}` → 删掉这个无用规则
- 所有 `/* ==== vX.Y.Z 标签 ==== */` 版本注释标记（内容保留，只删注释标签）

### 5. 压缩 CSS（仅在可能时）
- 重复的 `margin:0;padding:0` 保持
- 不要过度压缩，保证可读性

**结果**：CSS 行数从 ~1300 行降至 ~800 行

---

## 第二部分：执行摘要重构

当前执行摘要在顶部但**不够抓人**。替换为：

```
（放在宅域·品牌叙事 tab 最前面，现有执行摘要位置）

📌 一句话
宅域·ZHAIYU = 宠物洗护入口 + 电竞氛围搭建 + 客户需求产品化系统

📌 核心数据（并排 3 个）
- 启动资金：19.73 万元
- 回本周期：12-18 个月
- 天使轮：50-80 万 · 预期 A 轮 3-5 倍

📌 差异化（2 行）
我们不只做服务。每一句客户原话都是产品原材料。首店 59.87㎡，1.8元/天/㎡，合同免租 2 个月。
```
注意：去掉原有的长段文字执行摘要，用上面这个紧凑结构替代。

---

## 第三部分：视觉呼吸感

### 6. 增加 section 间距
- `.tab-panel` 内部的 section-card 之间：从 `margin-bottom:22px` 改为 `28px`
- `.z-narrative-grid` 底部加 margin-bottom: 32px
- `.z-product-grid` 底部加 margin-bottom: 32px  
- 每个 h2.z-h2 上面间距从 32px 改为 40px

### 7. 投资人 QA 卡片差异化
每个 QA 卡片的顶部色条改为对应不同颜色：
- Q1 资金：橙色
- Q2 平台/实体：蓝色
- Q3 门店必要：棕色
- Q4 洗护差异：绿色
- Q5 视频获客：紫色
- Q6 赚钱逻辑：橙色
- Q7 样板间：蓝色
- Q8 品类扩张：棕色
- Q9 首月证明：绿色

在每个 `.z-qa-card` 上加内联样式 `style="--qa-accent: #E85D3C"` 等，用伪类 `::before` 背景色继承 var(--qa-accent)

### 8. 响应式优化
- 选品矩阵（.z-product-grid）: 桌面 4 列 → 平板 2 列 → 手机 1 列
- 叙事卡（.z-narrative-grid）: 桌面 2 列 → 手机 1 列
- values wall: 桌面 4 列 → 手机 2 列
- QA 卡片: 桌面 2 列 → 手机 1 列
- 确保 640px 断点和 960px 断点都处理好

---

## 第四部分：内容精炼

### 9. "8 句话讲清" → "6 句话讲清"
合并/删除第 4 句"本地够近 + 服务够深 + 信任够强"（与第 5 句"实体店 = 电商信任补丁"内容重叠）
合并/删除第 6 句"吃大厂所不屑 · 做自有品牌"（与第 3 句"前期积累用户 · 后期选品卖货"重复）
保留顺序：01→02→03→05→07→08（洗护差异化→现金流优先→积累用户→实体店信任→客户档案→产品化）

注意：同时更新 HTML 中的编号（1-6）和 幻灯片 slides 中的对应内容。

### 10. 选品矩阵精简
从 8 个删到 6 个：删掉"扫地机器人真机跑道"和"降噪耳机隔音房"（这两个与核心宠物+电竞场景关联度最低）

### 11. 投资人问答精简
现有 9 个 Q&A → 精简为 7 个（删 Q7 电竞样板间/桌游馆、Q8 品类扩张，这两点在品牌叙事里已经够透）

---

## 执行方式
直接修改 `bp.html`。不删任何功能，只优化视觉/代码/内容。

完成后顶部注释改为：
`<!-- 投资人清洗版：2026-07-06 全面优化 -->`

开始。
