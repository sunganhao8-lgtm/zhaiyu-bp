{
  "name": "zhaiyu-bp",
  "version": "1.0.0",
  "private": true,
  "license": "UNLICENSED",
  "type": "module",
  "scripts": {
    "build": "esbuild src/light-home.js --bundle --format=esm --target=es2022 --minify --tree-shaking=true --sourcemap --outdir=assets --entry-names=light-home",
    "check": "node --check src/light-home.js",
    "build:legacy": "esbuild src/light-home.js --bundle --format=esm --target=es2022 --minify --legal-comments=inline --outfile=assets/light-home.js"
  },
  "dependencies": {
    "three": "0.185.1",
    "three-html-render": "0.1.2"
  },
  "devDependencies": {
    "esbuild": "0.25.12"
  }
}
